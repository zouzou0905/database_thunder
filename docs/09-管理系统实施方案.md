# 管理系统实施方案

> 版本: 1.0 | 日期: 2026-07-01

## 1. 概述

在现有项目基础上新增独立管理系统，提供账号管理和错误日志两大功能。系统与现有后端物理隔离（独立进程、独立端口），共享同一 PostgreSQL 数据库。

### 架构

```
Port 8000 (React SPA)    Port 8001 (FastAPI JSON API)    Port 8002 (Admin SSR)
       |                         |                              |
       +---- 运营端 -------------+         +---- 管理端 ---------+
                                                |
                                          Jinja2 服务端渲染
                                          Session 认证
                                          用户 CRUD + 错误查看器
```

### 技术选型

| 维度 | 选择 | 理由 |
|------|------|------|
| Web 框架 | FastAPI | 与现有后端一致 |
| UI 渲染 | Jinja2 服务端渲染 | 无 React 依赖，真正隔离 |
| 认证 | Starlette SessionMiddleware | 浏览器自动携带 cookie，无需 token 管理 |
| 数据库 | PostgreSQL（共用） | 同一 keyword_trends 库 |
| 连接池 | 独立 psycopg_pool | 与 backend 完全分离 |

---

## 2. 目录结构

```
admin/                                  # 管理系统根目录
  start_admin.ps1                       # 启动脚本（PowerShell）
  app/
    __init__.py
    main.py                             # FastAPI app 工厂
    pool.py                             # 独立连接池
    db.py                               # get_connection() context manager
    core/
      __init__.py
      config.py                         # AdminSettings 配置类
      auth.py                           # get_current_admin() 依赖注入
    routes/
      __init__.py
      auth.py                           # 登录/登出
      dashboard.py                      # 仪表盘首页
      users.py                          # 用户 CRUD
      error_logs.py                     # 错误日志查看
    templates/
      base.html                         # 基础布局（侧边栏 + flash 消息）
      login.html                        # 登录页
      dashboard.html                    # 仪表盘
      users/
        list.html                       # 用户列表
        create.html                     # 创建用户
        edit.html                       # 编辑用户
      error_logs/
        list.html                       # 错误列表
        detail.html                     # 错误详情 + traceback
    static/
      style.css                         # 管理端样式
      admin.js                          # 轻量 JS 辅助
```

---

## 3. 数据库新增表

### 3.1 error_logs — 错误日志

文件：`sql/schema/008_error_logs.sql`

```sql
CREATE TABLE IF NOT EXISTS error_logs (
    id              BIGSERIAL PRIMARY KEY,
    level           TEXT NOT NULL DEFAULT 'ERROR',
    source          TEXT NOT NULL DEFAULT 'backend',
    endpoint        TEXT,
    method          TEXT,
    status_code     INTEGER,
    message         TEXT NOT NULL,
    traceback       TEXT,
    request_body    TEXT,
    user_agent      TEXT,
    client_ip       TEXT,
    user_account    TEXT,
    resolved        BOOLEAN NOT NULL DEFAULT FALSE,
    created_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_error_logs_created
    ON error_logs (resolved, created_at DESC);

CREATE INDEX IF NOT EXISTS idx_error_logs_level
    ON error_logs (level, created_at DESC);

DO $$
BEGIN
    IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'keyword_user') THEN
        GRANT SELECT, INSERT, UPDATE, DELETE ON error_logs TO keyword_user;
        GRANT USAGE, SELECT ON SEQUENCE error_logs_id_seq TO keyword_user;
    END IF;
END $$;
```

### 3.2 login_history — 登录历史

文件：`sql/schema/009_login_history.sql`

```sql
CREATE TABLE IF NOT EXISTS login_history (
    id          BIGSERIAL PRIMARY KEY,
    user_id     BIGINT REFERENCES app_users(id),
    account     TEXT NOT NULL,
    login_at    TIMESTAMP NOT NULL DEFAULT NOW(),
    client_ip   TEXT,
    user_agent  TEXT,
    success     BOOLEAN NOT NULL DEFAULT TRUE
);

CREATE INDEX IF NOT EXISTS idx_login_history_user
    ON login_history (user_id, login_at DESC);

CREATE INDEX IF NOT EXISTS idx_login_history_time
    ON login_history (login_at DESC);

DO $$
BEGIN
    IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'keyword_user') THEN
        GRANT SELECT, INSERT, UPDATE, DELETE ON login_history TO keyword_user;
        GRANT USAGE, SELECT ON SEQUENCE login_history_id_seq TO keyword_user;
    END IF;
END $$;
```

### 3.3 注册迁移

修改 `scripts/init_db.py`，在 `SQL_FILES` 列表中添加：

```python
"sql/schema/008_error_logs.sql",
"sql/schema/009_login_history.sql",
```

---

## 4. 后端改造

### 4.1 错误捕获中间件

文件：`backend/app/error_capture.py`

```python
"""Middleware that writes unhandled exceptions to the error_logs table."""
from __future__ import annotations

import traceback

from starlette.middleware.base import BaseHTTPMiddleware
from starlette.requests import Request
from starlette.responses import Response

from app.pool import get_pool


class ErrorCaptureMiddleware(BaseHTTPMiddleware):
    """Catch unhandled exceptions and persist structured error data.

    Inserted early in the middleware stack so errors from any router or
    dependency are captured.  Only 5xx (unhandled) exceptions are logged;
    expected HTTPException (4xx) responses are passed through unchanged.
    """

    async def dispatch(self, request: Request, call_next) -> Response:
        try:
            return await call_next(request)
        except Exception as exc:
            await self._persist_error(request, exc)
            raise

    async def _persist_error(self, request: Request, exc: Exception) -> None:
        try:
            body: str | None = None
            try:
                raw = await request.body()
                if raw:
                    body = raw.decode("utf-8", errors="replace")[:2000]
            except Exception:
                pass

            client_ip: str | None = None
            if request.client:
                client_ip = request.client.host

            user_agent: str | None = (request.headers.get("user-agent", "") or "")[:500]

            user_account: str | None = None
            auth_header = request.headers.get("authorization", "")
            if auth_header.startswith("Bearer "):
                try:
                    from app.core.security import decode_access_token

                    payload = decode_access_token(auth_header[7:])
                    if payload:
                        user_account = payload.get("account")
                except Exception:
                    pass

            pool = get_pool()
            with pool.connection() as conn:
                with conn.cursor() as cur:
                    cur.execute(
                        """
                        INSERT INTO error_logs
                            (level, source, endpoint, method, status_code,
                             message, traceback, request_body,
                             user_agent, client_ip, user_account)
                        VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
                        """,
                        [
                            "ERROR",
                            "backend",
                            str(request.url.path),
                            request.method,
                            500,
                            str(exc)[:2000],
                            traceback.format_exc()[:10000],
                            body,
                            user_agent,
                            client_ip,
                            user_account,
                        ],
                    )
                conn.commit()
        except Exception:
            pass  # never let error logging itself cause failures
```

### 4.2 注册中间件

文件：`backend/app/main.py`

在 `create_app()` 函数中，CORS 中间件之后、所有 router 之前添加：

```python
from app.error_capture import ErrorCaptureMiddleware

# 在 app.add_middleware(CORSMiddleware, ...) 之后：
app.add_middleware(ErrorCaptureMiddleware)
```

### 4.3 登录历史

文件：`backend/app/api/auth.py`

在 `login` 函数成功返回前，插入一条登录记录。在 return 语句之前：

```python
# 记录登录历史
try:
    with conn.cursor() as cur:
        cur.execute(
            """
            INSERT INTO login_history (user_id, account, client_ip, user_agent, success)
            VALUES (%s, %s, %s, %s, TRUE)
            """,
            [user["id"], user["account"],
             None,  # client_ip 在 FastAPI 中通过 Request 获取，此处可选
             None], # user_agent 同理
        )
    conn.commit()
except Exception:
    pass  # 不影响登录流程
```

> 注意：完整实现需要从 Request 对象获取 client_ip 和 user_agent。由于当前 `/auth/login` 路由不接收 Request 参数，简化处理为 NULL。后续可优化。

---

## 5. 管理系统实现

### 5.1 启动脚本

文件：`admin/start_admin.ps1`

```powershell
$ErrorActionPreference = "Stop"
$projectRoot = Split-Path -Parent $PSScriptRoot
Set-Location $projectRoot

$env:PYTHONPATH = "admin;backend"

.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8002
```

### 5.2 配置

文件：`admin/app/core/config.py`

```python
from __future__ import annotations

import os
from dataclasses import dataclass
from pathlib import Path

from backend.app.core.config import load_env_file


@dataclass(frozen=True)
class AdminSettings:
    app_name: str
    database_url: str
    session_secret: str
    templates_dir: Path
    static_dir: Path


def get_admin_settings() -> AdminSettings:
    load_env_file()
    database_url = os.environ.get("DATABASE_URL")
    if not database_url:
        raise RuntimeError("DATABASE_URL not set")

    admin_root = Path(__file__).resolve().parent.parent  # admin/app/

    return AdminSettings(
        app_name=os.environ.get("APP_NAME", "Keyword Trends") + " Admin",
        database_url=database_url,
        session_secret=os.environ.get(
            "ADMIN_SESSION_SECRET", "admin-dev-change-me"
        ),
        templates_dir=admin_root / "templates",
        static_dir=admin_root / "static",
    )
```

### 5.3 连接池

文件：`admin/app/pool.py`

```python
from __future__ import annotations

import psycopg
from psycopg_pool import ConnectionPool

from admin.app.core.config import get_admin_settings

_pool: ConnectionPool | None = None


def get_pool() -> ConnectionPool:
    global _pool
    if _pool is not None:
        return _pool
    settings = get_admin_settings()
    _pool = ConnectionPool(
        conninfo=settings.database_url,
        min_size=2,
        max_size=10,
        timeout=30,
        max_lifetime=3600,
        kwargs={"row_factory": None},
        open=True,
    )
    return _pool


def close_pool() -> None:
    global _pool
    if _pool is not None:
        _pool.close()
        _pool = None
```

### 5.4 数据库连接

文件：`admin/app/db.py`

```python
from __future__ import annotations

from collections.abc import Iterator

import psycopg
from psycopg.rows import dict_row

from admin.app.pool import get_pool


def get_connection() -> Iterator[psycopg.Connection]:
    pool = get_pool()
    with pool.connection() as conn:
        conn.row_factory = dict_row
        yield conn
```

### 5.5 认证

文件：`admin/app/core/auth.py`

```python
from __future__ import annotations

from typing import Any

from fastapi import Depends, HTTPException, Request
from psycopg.rows import dict_row

from admin.app.pool import get_pool


def get_current_admin(request: Request) -> dict[str, Any]:
    """从 session 读取 admin 用户，并验证其仍然有效。"""
    user = request.session.get("admin_user")
    if not user:
        raise HTTPException(status_code=401, detail="Not authenticated")

    pool = get_pool()
    with pool.connection() as conn:
        conn.row_factory = dict_row
        with conn.cursor() as cur:
            cur.execute(
                """
                SELECT id, account, display_name, role, is_active
                FROM app_users
                WHERE id = %s
                """,
                [user["id"]],
            )
            row = cur.fetchone()

    if not row or not row["is_active"] or row["role"] != "admin":
        request.session.clear()
        raise HTTPException(status_code=401, detail="Account disabled or not admin")

    return dict(row)
```

### 5.6 FastAPI 应用工厂

文件：`admin/app/main.py`

```python
from __future__ import annotations

from fastapi import FastAPI
from fastapi.staticfiles import StaticFiles
from starlette.middleware.sessions import SessionMiddleware

from admin.app.core.config import get_admin_settings
from admin.app.routes import auth, dashboard, error_logs, users

settings = get_admin_settings()


def create_admin_app() -> FastAPI:
    app = FastAPI(title=settings.app_name)

    app.add_middleware(
        SessionMiddleware,
        secret_key=settings.session_secret,
        max_age=12 * 3600,
        https_only=False,
    )

    app.mount("/static", StaticFiles(directory=str(settings.static_dir)), name="static")

    app.include_router(auth.router)
    app.include_router(dashboard.router)
    app.include_router(users.router)
    app.include_router(error_logs.router)

    return app


app = create_admin_app()
```

### 5.7 路由：登录/登出

文件：`admin/app/routes/auth.py`

```python
from __future__ import annotations

from fastapi import APIRouter, Depends, Form, Request
from fastapi.responses import RedirectResponse
from psycopg import Connection

from admin.app.core.auth import get_current_admin
from admin.app.db import get_connection
from backend.app.core.security import verify_password
from backend.app.services.users import get_user_by_account

router = APIRouter(prefix="/admin", tags=["admin-auth"])


@router.get("/login")
def login_page(request: Request):
    return _templates().TemplateResponse("login.html", {"request": request})


@router.post("/login")
def login(
    request: Request,
    account: str = Form(...),
    password: str = Form(...),
    conn: Connection = Depends(get_connection),
):
    user = get_user_by_account(conn, account)
    if not user or not user["is_active"] or user["role"] != "admin":
        return _templates().TemplateResponse(
            "login.html",
            {"request": request, "error": "账号不存在、已停用或非管理员"},
            status_code=401,
        )
    if not verify_password(password, user["password_hash"]):
        return _templates().TemplateResponse(
            "login.html",
            {"request": request, "error": "密码不正确"},
            status_code=401,
        )
    request.session["admin_user"] = {
        "id": user["id"],
        "account": user["account"],
        "display_name": user["display_name"],
        "role": user["role"],
    }
    return RedirectResponse(url="/admin", status_code=303)


@router.get("/logout")
def logout(request: Request):
    request.session.clear()
    return RedirectResponse(url="/admin/login", status_code=303)


def _templates():
    from admin.app.core.config import get_admin_settings
    from fastapi.templating import Jinja2Templates

    return Jinja2Templates(directory=str(get_admin_settings().templates_dir))
```

### 5.8 路由：仪表盘

文件：`admin/app/routes/dashboard.py`

```python
from __future__ import annotations

from fastapi import APIRouter, Depends, Request
from fastapi.templating import Jinja2Templates
from psycopg import Connection

from admin.app.core.auth import get_current_admin
from admin.app.core.config import get_admin_settings
from admin.app.db import get_connection

router = APIRouter(prefix="/admin", tags=["admin-dashboard"])
templates = Jinja2Templates(directory=str(get_admin_settings().templates_dir))


@router.get("")
def dashboard(
    request: Request,
    admin=Depends(get_current_admin),
    conn: Connection = Depends(get_connection),
):
    with conn.cursor() as cur:
        cur.execute("SELECT COUNT(*) AS total FROM app_users")
        total_users = cur.fetchone()["total"]
        cur.execute("SELECT COUNT(*) AS total FROM app_users WHERE is_active = TRUE")
        active_users = cur.fetchone()["total"]
        cur.execute(
            "SELECT COUNT(*) AS total FROM error_logs WHERE resolved = FALSE"
        )
        unresolved_errors = cur.fetchone()["total"]
        cur.execute(
            """
            SELECT id, account, display_name, role, is_active, created_at
            FROM app_users
            ORDER BY created_at DESC
            LIMIT 5
            """
        )
        recent_users = cur.fetchall()
        cur.execute(
            """
            SELECT id, level, endpoint, message, created_at
            FROM error_logs
            WHERE resolved = FALSE
            ORDER BY created_at DESC
            LIMIT 5
            """
        )
        recent_errors = cur.fetchall()

    return templates.TemplateResponse(
        "dashboard.html",
        {
            "request": request,
            "admin": admin,
            "total_users": total_users,
            "active_users": active_users,
            "unresolved_errors": unresolved_errors,
            "recent_users": recent_users,
            "recent_errors": recent_errors,
        },
    )
```

### 5.9 路由：用户管理

文件：`admin/app/routes/users.py`

完整 CRUD 路由，包含：
- `GET /admin/users` — 用户列表（分页）
- `GET /admin/users/create` — 创建表单
- `POST /admin/users` — 执行创建
- `GET /admin/users/{id}/edit` — 编辑表单
- `POST /admin/users/{id}` — 执行更新
- `POST /admin/users/{id}/toggle` — 启禁用切换
- `POST /admin/users/{id}/reset-password` — 重置密码

```python
from __future__ import annotations

from fastapi import APIRouter, Depends, Form, HTTPException, Request
from fastapi.responses import RedirectResponse
from fastapi.templating import Jinja2Templates
from psycopg import Connection

from admin.app.core.auth import get_current_admin
from admin.app.core.config import get_admin_settings
from admin.app.db import get_connection
from backend.app.core.security import hash_password
from backend.app.services.users import create_user, list_users, update_user

router = APIRouter(prefix="/admin/users", tags=["admin-users"])
templates = Jinja2Templates(directory=str(get_admin_settings().templates_dir))


@router.get("")
def user_list(
    request: Request,
    page: int = 1,
    admin=Depends(get_current_admin),
    conn: Connection = Depends(get_connection),
):
    users = list_users(conn)
    return templates.TemplateResponse(
        "users/list.html",
        {"request": request, "admin": admin, "users": users},
    )


@router.get("/create")
def create_form(request: Request, admin=Depends(get_current_admin)):
    return templates.TemplateResponse(
        "users/create.html",
        {"request": request, "admin": admin},
    )


@router.post("")
def create(
    request: Request,
    account: str = Form(...),
    display_name: str = Form(...),
    password: str = Form(...),
    role: str = Form("operator"),
    admin=Depends(get_current_admin),
    conn: Connection = Depends(get_connection),
):
    try:
        create_user(conn, account=account, display_name=display_name, password=password, role=role)
    except Exception as e:
        return templates.TemplateResponse(
            "users/create.html",
            {"request": request, "admin": admin, "error": str(e)},
        )
    return RedirectResponse(url="/admin/users", status_code=303)


@router.get("/{user_id}/edit")
def edit_form(
    user_id: int,
    request: Request,
    admin=Depends(get_current_admin),
    conn: Connection = Depends(get_connection),
):
    from backend.app.services.users import get_user_by_id
    user = get_user_by_id(conn, user_id)
    if not user:
        raise HTTPException(status_code=404, detail="User not found")
    return templates.TemplateResponse(
        "users/edit.html",
        {"request": request, "admin": admin, "user": user},
    )


@router.post("/{user_id}")
def update(
    user_id: int,
    request: Request,
    display_name: str = Form(...),
    role: str = Form(...),
    is_active: str = Form("true"),
    new_password: str = Form(""),
    admin=Depends(get_current_admin),
    conn: Connection = Depends(get_connection),
):
    kwargs = {
        "display_name": display_name,
        "role": role,
        "is_active": is_active == "true",
    }
    if new_password.strip():
        kwargs["password"] = new_password.strip()
    update_user(conn, user_id, **kwargs)
    return RedirectResponse(url="/admin/users", status_code=303)


@router.post("/{user_id}/toggle")
def toggle_active(
    user_id: int,
    admin=Depends(get_current_admin),
    conn: Connection = Depends(get_connection),
):
    from backend.app.services.users import get_user_by_id
    user = get_user_by_id(conn, user_id)
    if not user:
        raise HTTPException(status_code=404)
    update_user(conn, user_id, is_active=not user["is_active"])
    return RedirectResponse(url="/admin/users", status_code=303)
```

### 5.10 路由：错误日志

文件：`admin/app/routes/error_logs.py`

```python
from __future__ import annotations

from fastapi import APIRouter, Depends, Request
from fastapi.templating import Jinja2Templates
from psycopg import Connection

from admin.app.core.auth import get_current_admin
from admin.app.core.config import get_admin_settings
from admin.app.db import get_connection

router = APIRouter(prefix="/admin/error-logs", tags=["admin-errors"])
templates = Jinja2Templates(directory=str(get_admin_settings().templates_dir))


@router.get("")
def list_errors(
    request: Request,
    page: int = 1,
    level: str = "",
    resolved: str = "",
    admin=Depends(get_current_admin),
    conn: Connection = Depends(get_connection),
):
    page_size = 50
    offset = (page - 1) * page_size

    where = ["1=1"]
    params: dict = {"limit": page_size, "offset": offset}

    if level:
        where.append("level = %(level)s")
        params["level"] = level
    if resolved == "yes":
        where.append("resolved = TRUE")
    elif resolved == "no":
        where.append("resolved = FALSE")

    where_sql = " AND ".join(where)

    with conn.cursor() as cur:
        cur.execute(
            f"SELECT COUNT(*) AS total FROM error_logs WHERE {where_sql}",
            {k: v for k, v in params.items() if k not in ("limit", "offset")},
        )
        total = cur.fetchone()["total"]

        cur.execute(
            f"""
            SELECT id, level, source, endpoint, method, status_code,
                   message, user_account, resolved, created_at
            FROM error_logs
            WHERE {where_sql}
            ORDER BY created_at DESC
            LIMIT %(limit)s OFFSET %(offset)s
            """,
            params,
        )
        items = cur.fetchall()

    total_pages = (total + page_size - 1) // page_size

    return templates.TemplateResponse(
        "error_logs/list.html",
        {
            "request": request,
            "admin": admin,
            "items": items,
            "page": page,
            "total_pages": total_pages,
            "total": total,
            "level": level,
            "resolved": resolved,
        },
    )


@router.get("/{error_id}")
def error_detail(
    error_id: int,
    request: Request,
    admin=Depends(get_current_admin),
    conn: Connection = Depends(get_connection),
):
    with conn.cursor() as cur:
        cur.execute("SELECT * FROM error_logs WHERE id = %s", [error_id])
        error = cur.fetchone()
    if not error:
        from fastapi import HTTPException
        raise HTTPException(status_code=404)
    return templates.TemplateResponse(
        "error_logs/detail.html",
        {"request": request, "admin": admin, "error": error},
    )


@router.post("/{error_id}/resolve")
def toggle_resolve(
    error_id: int,
    admin=Depends(get_current_admin),
    conn: Connection = Depends(get_connection),
):
    with conn.cursor() as cur:
        cur.execute(
            "UPDATE error_logs SET resolved = NOT resolved WHERE id = %s RETURNING id",
            [error_id],
        )
    conn.commit()
    from fastapi.responses import RedirectResponse
    return RedirectResponse(url=f"/admin/error-logs/{error_id}", status_code=303)
```

### 5.11 HTML 模板

所有模板使用 Jinja2 语法，继承 `base.html`。

**base.html** — 基础布局骨架：
- 左侧深色侧边栏：Dashboard / 用户管理 / 错误日志 / 退出
- 右侧白色内容区：`{% block content %}`
- 顶部 flash 消息条（如有）
- 引入 `/static/style.css` 和 `/static/admin.js`

**login.html** — 居中卡片式登录表单：
- 账号输入框
- 密码输入框
- 错误提示（红色文字）
- 登录按钮

**dashboard.html** — 4 个统计卡片 + 最近记录：
- 总用户数 / 活跃用户数 / 未处理错误数 / 管理员数
- 最近 5 个新增用户
- 最近 5 条未处理错误

**users/list.html** — 全宽表格：
- 列：账号 / 显示名 / 角色 / 状态 / 创建时间 / 操作
- 操作：编辑 / 启禁用切换 / 重置密码
- 右上角「创建用户」按钮

**users/create.html** — 表单：
- 账号（必填，2-50 字符）
- 显示名（必填，1-100 字符）
- 密码（必填，至少 6 字符）
- 角色（下拉：admin / manager / operator / viewer）

**users/edit.html** — 同上，预填值：
- 额外：is_active 开关
- 额外：新密码（可选，留空不修改）

**error_logs/list.html** — 表格：
- 列：级别 / 时间 / 端点 / 消息（截断）/ 用户 / 状态 / 操作
- 筛选：级别下拉（ERROR / WARNING）+ 已处理/未处理切换
- 分页

**error_logs/detail.html** — 详情面板：
- 元数据网格：级别、状态码、端点、方法、时间、用户、IP、UA
- 错误消息
- `<pre>` 格式化的 traceback
- 请求 body（如有）
- 「标记已处理/未处理」按钮

### 5.12 静态资源

**style.css** — 约 200 行，核心样式：
- 颜色变量：`--sidebar-bg: #1a1a2e`, `--accent: #4a90d9`, `--danger: #e74c3c`, `--success: #27ae60`
- 布局：左侧 240px 固定侧边栏 + 右侧自适应内容区
- 表格：斑马纹、悬停高亮
- 表单：统一 input/select/button 样式
- 标签：`.badge` 用于角色和状态显示
- Flash 消息：绿色成功条 + 红色错误条，自动消失动画

**admin.js** — 约 50 行：
- 删除/敏感操作前 `confirm()` 确认
- Flash 消息 5 秒后自动淡出
- 密码可见性切换按钮

---

## 6. 环境配置

### 6.1 .env 新增

```
ADMIN_SESSION_SECRET=admin_thunder_change_me_2026
```

### 6.2 requirements.txt 新增依赖

```
jinja2>=3.1
python-multipart>=0.0.18
itsdangerous>=2.2
```

安装：
```powershell
.\.venv\Scripts\pip.exe install jinja2 python-multipart itsdangerous
```

---

## 7. 启动方式

```powershell
# 1. 运行数据库迁移
python scripts/init_db.py

# 2. 启动管理系统
.\admin\start_admin.ps1
```

访问：`http://192.168.0.135:8002/admin`（LAN）或 `http://localhost:8002/admin`（本机）

---

## 8. 验收清单

- [ ] `init_db.py` 成功创建 error_logs 和 login_history 表
- [ ] `admin/start_admin.ps1` 正常启动，端口 8002
- [ ] 浏览器访问 `/admin/login`，`admin / 123456` 可登录
- [ ] 仪表盘显示正确的统计数字
- [ ] 创建用户：输入账号/显示名/密码/角色，成功后跳转列表
- [ ] 编辑用户：修改显示名/角色，切换启用状态
- [ ] 重置密码：输入新密码，旧密码失效新密码生效
- [ ] 触发后端错误（如请求不存在的接口），刷新错误日志页能看到记录
- [ ] 错误详情页显示完整 traceback 和请求信息
- [ ] 标记已处理/未处理正常切换
- [ ] 退出登录后无法直接访问 `/admin`，自动跳转登录页
